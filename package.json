{
  "name": "@repo/content-manager",
  "version": "0.0.0",
  "license": "MIT",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": [
    "dist/**",
    "src/**"
  ],
  "scripts": {
    "build": "tsdown",
    "build:watch": "tsdown --watch",
    "dev": "tsdown --watch",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist",
    "lint": "biome check --write .",
    "lint:check": "biome check .",
    "format": "biome format --write .",
    "format:check": "biome format ."
  },
  "dependencies": {
    "@repo/content-blocks": "workspace:*",
    "@repo/content-extractors": "workspace:*",
    "@repo/content-scanner": "workspace:*",
    "@repo/indexer-api": "workspace:*",
    "@statewalker/webrun-files": "catalog:"
  },
  "devDependencies": {
    "@repo/indexer-mem-flexsearch": "workspace:*",
    "tsdown": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
