/**
 * Re-exports from the Vercel AI SDK for defining tools.
 * Consumers should import `tool` and `ToolSet` from here
 * instead of depending on "ai" directly.
 */
export { type ToolSet, tool } from "ai";
